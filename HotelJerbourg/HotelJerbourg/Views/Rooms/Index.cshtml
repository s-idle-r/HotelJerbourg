@*@model IEnumerable<HotelJerbourg.Models.Room>*@
@model HotelJerbourg.Models.ModelsVM

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Room.Number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Room.Availability)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomCategory.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.RoomList) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Availability)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoomCategories.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoomID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoomID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoomID })
        </td>
    </tr>
}

</table>
